# Legacy GUI

`saga_gui.py` is the original SAGA-derived segmentation interface retained for
reference. It is not the supported application entry point.

Run the current GUI from the repository root:

```bash
python rt_gs_gui_v3.py -m /absolute/model/path
```
